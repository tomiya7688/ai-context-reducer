{
  "name": "upd-commander",
  "layers": [
    {"name": "ui", "patterns": ["**/ui/**", "**/UI/**"]},
    {"name": "process", "patterns": ["**/process/**", "**/Process/**"]},
    {"name": "data", "patterns": ["**/data/**", "**/Data/**"]}
  ],
  "roles": [
    {"name": "commander", "patterns": ["**/*commander*", "**/*Commander*"]},
    {"name": "messenger", "patterns": ["**/*messenger*", "**/*Messenger*"]},
    {"name": "processing", "patterns": ["**/*processing*", "**/*Processing*"]}
  ],
  "boundary_roles": ["messenger"],
  "routing": {
    "same_layer_first": true,
    "cross_layer_via_boundary_only": true,
    "expand_on_contract_change": true
  },
  "notes": [
    "Commander routes work but should not own business processing.",
    "Messenger represents the normal cross-layer communication boundary.",
    "Processing owns concrete work inside its layer.",
    "Cross-layer expansion should be conservative unless a message/contract boundary changes."
  ]
}
